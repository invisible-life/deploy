# Traefik middleware for path stripping
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: stripprefix
  namespace: invisible
spec:
  stripPrefix:
    prefixes:
      - /chat
      - /api
      - /supabase