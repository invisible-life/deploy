apiVersion: v1
kind: ConfigMap
metadata:
  name: url-config
  labels:
    app.kubernetes.io/name: invisible
    app.kubernetes.io/component: config
data:
  # These values will be set by the deployment script based on domain/IP configuration
  API_PUBLIC_URL: "http://localhost:4300"
  SUPABASE_PUBLIC_URL: "http://localhost:8000"
  SITE_URL: "http://localhost:3000"
  API_EXTERNAL_URL: "http://localhost:8000"