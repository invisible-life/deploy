apiVersion: apps/v1
kind: Deployment
metadata:
  name: ollama
  labels:
    app.kubernetes.io/name: ollama
    app.kubernetes.io/component: llm
spec:
  replicas: 1
  revisionHistoryLimit: 2  # Keep only 2 old replica sets for rollback
  selector:
    matchLabels:
      app.kubernetes.io/name: ollama
      app.kubernetes.io/component: llm
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ollama
        app.kubernetes.io/component: llm
        app.kubernetes.io/managed-by: kustomize
        app.kubernetes.io/part-of: invisible-platform
        environment: production
    spec:
      initContainers:
      - name: pull-models
        image: ollama/ollama:latest
        command: 
        - sh
        - -c
        - |
          ollama serve &
          sleep 5
          ollama pull deepseek-r1:8b || true
          ollama pull llama3.2:3b || true
        volumeMounts:
        - name: ollama-data
          mountPath: /root/.ollama
      containers:
      - name: ollama
        image: ollama/ollama:latest
        
        imagePullPolicy: Always
        ports:
        - containerPort: 11434
          name: http
        env:
        - name: OLLAMA_HOST
          value: "0.0.0.0"
        - name: OLLAMA_DEBUG
          value: "1"
        volumeMounts:
        - name: ollama-data
          mountPath: /root/.ollama
        livenessProbe:
          httpGet:
            path: /
            port: 11434
          initialDelaySeconds: 30
          periodSeconds: 30
        resources:
          requests:
            cpu: 8000m     # 8 CPU cores (40% of total)
            memory: 32Gi   # 32GB RAM - half the machine's memory
          limits:
            cpu: 14000m    # Allow bursting to 14 cores (70% of total)
            memory: 48Gi   # Max 48GB RAM - 77% of total
      volumes:
      - name: ollama-data
        persistentVolumeClaim:
          claimName: ollama-data